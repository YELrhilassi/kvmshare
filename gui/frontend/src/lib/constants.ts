// Shared defaults. Must stay in sync with the Rust server
// (crates/app/src/lib.rs: DEFAULT_PORT / DEFAULT_SCREEN_W / H) and the
// Go GUI backend (gui/config.go).
export const DEFAULT_PORT = 24800;
export const DEFAULT_SCREEN_WIDTH = 1920;
export const DEFAULT_SCREEN_HEIGHT = 1080;
# Rust build output. Cargo.lock is NOT ignored: this workspace ships
# binaries, so the lockfile is part of a reproducible build.
/target

# Go / Wails
gui/build/bin/
gui/frontend/dist/
gui/frontend/node_modules/
gui/kvmshare-gui
gui/kvmshare-gui.exe
gui/kvmshare-install
gui/kvmshare-install.exe
gui/kvmshare-server.log

# Release artifacts (make release)
/dist

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store